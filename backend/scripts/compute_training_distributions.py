"""
Compute reference distributions from the training dataset.

Generates a JSON file with per-feature statistics (mean, std, quantiles)
used as the baseline for feature drift monitoring.

Usage:
    python scripts/compute_training_distributions.py
"""

# Implementation will be added in Phase 11.

if __name__ == "__main__":
    print("Training distribution computation — to be implemented in Phase 11")
